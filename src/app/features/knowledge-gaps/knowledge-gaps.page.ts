import { CommonModule } from '@angular/common';import { Component,OnInit } from '@angular/core';import { KnowledgeGapsService } from './knowledge-gaps.service';
import { KnowledgeService } from '../knowledge/knowledge.service';import { PageHeader } from '../../shared/ui';@Component({standalone:true,imports:[CommonModule,PageHeader],template:`<qai-page-header title="Knowledge Gaps" subtitle="Turn unanswered questions into better automation coverage."><button (click)="load()">↻ Analyze</button></qai-page-header><div class="gap-cards"><article *ngFor="let g of rows"><header><span class="pill hot" *ngIf="g.impactScore>=70">High impact</span><span class="pill" *ngIf="g.impactScore<70">Medium impact</span><b>{{g.occurrences}} asks</b></header><h3>{{g.topic}}</h3><p>“{{g.exampleQuestion}}”</p><footer><button (click)="createArticle(g)">Create knowledge article</button><button (click)="resolve(g)">Mark resolved</button></footer></article></div>`})export class KnowledgeGapsPage implements OnInit{rows:any[]=[];constructor(private data:KnowledgeGapsService,private knowledge:KnowledgeService){}ngOnInit(){this.load()}load(){this.data.list<any[]>().subscribe(r=>this.rows=r)}createArticle(g:any){this.knowledge.createDocument({title:g.topic,body:'',published:false,version:1}).subscribe(()=>alert('Draft article created. Open Knowledge to complete it.'))}resolve(g:any){this.data.resolve<any>(g.id,{...g,status:'resolved'}).subscribe({next:r=>Object.assign(g,r),error:()=>alert('Could not resolve this knowledge gap. Nothing was changed.')})}}