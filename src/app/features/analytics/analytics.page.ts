import { CommonModule } from '@angular/common';import { Component,OnInit } from '@angular/core';import { PageHeader } from '../../shared/ui';import { AnalyticsService } from './analytics.service';
@Component({standalone:true,imports:[CommonModule,PageHeader],template:`<qai-page-header title="Analytics & ROI" subtitle="Measure support efficiency, pipeline creation and Automation-influenced revenue."><button (click)="load()">↻ Refresh</button></qai-page-header><div class="metrics"><article><span>Qualified leads</span><strong>{{d.qualified||0}}</strong><small>{{d.hot||0}} hot</small></article><article><span>Open pipeline</span><strong>{{money(d.pipeline||0)}}</strong><small>Automation influenced</small></article><article><span>Won revenue</span><strong>{{money(d.won||0)}}</strong></article><article><span>Automated conversations</span><strong>{{d.aiConversations||0}}</strong></article><article><span>Tickets</span><strong>{{d.tickets||0}}</strong></article></div><div class="grid2"><section class="panel"><header><b>Revenue attribution</b><span>{{attribution.length}} records</span></header><table><thead><tr><th>Model</th><th>Influenced revenue</th><th>Opportunity</th><th>Created</th></tr></thead><tbody><tr *ngFor="let x of attribution"><td>{{x.model}}</td><td><b>{{money(x.influencedRevenue)}}</b></td><td>{{x.opportunityId||'—'}}</td><td>{{x.createdAtUtc|date:'short'}}</td></tr></tbody></table></section><section class="panel"><header><b>Revenue automation engine</b></header><div class="impact"><div><b>{{d.hot||0}}</b><span>Hot leads</span></div><div><b>{{money(totalAttributed)}}</b><span>Attributed pipeline</span></div><div><b>{{conversion}}%</b><span>Pipeline won</span></div><div><b>{{d.aiConversations||0}}</b><span>Automated conversations</span></div></div></section></div>`})
export class AnalyticsPage implements OnInit{d:any={};attribution:any[]=[];constructor(private data:AnalyticsService){}ngOnInit(){this.load()}load(){this.data.overview().subscribe(r=>this.d=r);this.data.revenue().subscribe(r=>this.attribution=r)}get totalAttributed(){return this.attribution.reduce((s,x)=>s+Number(x.influencedRevenue||0),0)}get conversion(){const p=Number(this.d.pipeline||0),w=Number(this.d.won||0);return p+w?Math.round(w/(p+w)*100):0}money(v:number){return new Intl.NumberFormat('de-DE',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(v||0)}}
