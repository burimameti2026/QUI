import { CommonModule } from '@angular/common';
import { Component, inject } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { RouterLink } from '@angular/router';
import { PageHeader } from '../../shared/ui';
import { RealWorkspaceOptions, RealWorkspaceRequest, RealWorkspaceResult, RealWorkspaceService } from './real-workspace.service';

@Component({standalone:true,imports:[CommonModule,FormsModule,RouterLink,PageHeader],template:`
<qai-page-header title="Prepare Real Workspace" subtitle="Select the target market once. The platform automatically prepares and starts the acquisition operation."></qai-page-header>
<div class="steps"><span [class.active]="step===1">1 Area</span><span [class.active]="step===2">2 Target</span><span [class.active]="step===3">3 Automation</span></div>
<section *ngIf="step===1" class="panel"><h2>What should this workspace do?</h2><p>Select the operating area. This determines which automation is provisioned for this tenant.</p><div class="grid"><button class="choice" *ngFor="let item of options?.useCases" [class.selected]="useCase===item.id" (click)="selectArea(item.id)"><b>{{item.name}}</b><small>{{item.description}}</small></button></div></section>
<section *ngIf="step===2" class="panel"><div class="heading"><div><h2>Define the target market</h2><p>You provide business context. Prospecting, enrichment, qualification and campaign routing happen automatically inside this tenant.</p></div><button class="secondary" (click)="step=1">Back</button></div><div class="form"><label>Template<select [(ngModel)]="templateKey"><option value="">Select template</option><option *ngFor="let t of templates" [value]="t.id">{{t.name}}</option></select></label><label>Industry<input [(ngModel)]="industry" placeholder="Logistics, SaaS, Manufacturing..."/></label><label>Region<input [(ngModel)]="region" placeholder="DACH, Europe, North America..."/></label><label>Countries<input [(ngModel)]="countries" placeholder="Germany, Austria, Switzerland"/></label><label>Workspace name<input [(ngModel)]="name" placeholder="Optional"/></label><label>Daily discovery limit<input type="number" [(ngModel)]="dailyDiscoveryLimit" min="1" max="100"/></label><label>Minimum qualification score<input type="number" [(ngModel)]="minimumScore" min="1" max="100"/></label></div><div class="automation"><b>Automation plan</b><div class="pipeline"><span>Configure Agent</span><i>→</i><span>Discover</span><i>→</i><span>Enrich</span><i>→</i><span>Qualify</span><i>→</i><span>Route to Campaign</span></div></div><div class="actions"><button [disabled]="!ready||saving" (click)="start()">{{saving?'Preparing automation…':'Prepare & Start Automation'}}</button></div></section>
<section *ngIf="step===3" class="panel"><div class="success-head"><div><span class="eyebrow">AUTOMATION CONTROL</span><h2>Automation started</h2><p>The workspace has been prepared for this tenant. The initial acquisition run is queued and the daily scheduler will continue automatically.</p></div><span class="state-pill">{{result?.status || 'activation-queued'}}</span></div><div class="result-grid"><div><small>Agent</small><strong>{{result?.agentName || '-'}}</strong><span>{{result?.agentStatus || '-'}}</span></div><div><small>Initial run</small><strong>{{result?.initialRunId || 'Already queued / not returned'}}</strong><span>Queued for autonomous discovery</span></div><div><small>Target list</small><strong>{{result?.targetListId || 'Provisioned / pending'}}</strong><span>Qualified prospects route here</span></div><div><small>Campaign</small><strong>{{result?.campaignId || 'Provisioned / pending'}}</strong><span>Delivery remains approval-controlled</span></div></div><div class="timeline"><div class="done"><b>Workspace configured</b><small>Selected template and target market applied to this tenant only</small></div><div class="done"><b>Autonomous agent active</b><small>{{result?.agentName}} · {{result?.agentStatus}}</small></div><div [class.done]="!!result?.initialRunId"><b>Initial prospecting queued</b><small>The first acquisition run is handled by the background worker.</small></div><div [class.done]="!!result?.targetListId"><b>Qualified target list ready</b><small>Qualified prospects enter the tenant target list automatically.</small></div><div [class.done]="!!result?.campaignId"><b>Acquisition campaign ready</b><small>Campaign workspace is provisioned; outreach remains governed by sender and approval configuration.</small></div><div class="done"><b>Daily automation enabled</b><small>New acquisition runs continue on the tenant's configured local schedule.</small></div></div><div class="actions"><a routerLink="/acquisition/autonomous">Open Acquisition</a><a routerLink="/discover" class="secondary-link">Open Prospects</a><a routerLink="/golden-pipeline" class="secondary-link">Open Golden Pipeline</a></div></section>
<p class="status" *ngIf="status" [class.error]="error">{{status}}</p>
`,styles:[`.steps{display:flex;gap:8px;margin-bottom:18px;flex-wrap:wrap}.steps span{padding:8px 12px;border:1px solid var(--border,#dfe5ed);border-radius:999px;font-size:12px}.steps .active{font-weight:700;border-color:#2563eb;background:#edf4ff;color:#315fbd}.panel{padding:24px;border:1px solid #dfe5ed;border-radius:14px;margin-bottom:18px;background:#f4f6f9;box-shadow:0 6px 18px #17203309}h2{margin:0 0 8px;color:#172033}.panel p{color:#68758a;font-size:11px;line-height:1.6}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:14px;margin-top:18px}.choice{text-align:left;padding:20px;border:1px solid #d9e0e9;border-radius:12px;background:#fff;color:#172033;cursor:pointer;box-shadow:0 4px 12px #17203308}.choice b,.choice small{display:block}.choice small{margin-top:8px;color:#68758a}.choice.selected{outline:2px solid #2563eb;border-color:#8eb1ed}.heading{display:flex;justify-content:space-between;gap:16px}.form{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px;margin-top:20px}.form label{font-size:10px;font-weight:700;color:#5f6c80}.form input,.form select{display:block;width:100%;box-sizing:border-box;margin-top:6px;padding:11px;border:1px solid #cfd8e5;border-radius:8px;background:#fff;color:#172033}.automation{margin-top:22px;padding:16px;border:1px solid #d9e0e9;border-radius:10px;background:#fff}.pipeline{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-top:12px}.pipeline span{padding:7px 10px;border:1px solid #d9e0e9;border-radius:999px;background:#f8fafc;font-size:10px;color:#536176}.pipeline i{font-style:normal;color:#9aa7b8}.actions{display:flex;gap:10px;justify-content:flex-end;margin-top:22px;flex-wrap:wrap}.actions button,.actions a{padding:11px 16px;border:0;border-radius:8px;background:linear-gradient(135deg,#2563eb,#315fbd);color:#fff;font-weight:800;text-decoration:none;cursor:pointer;font-size:10px}.actions button:disabled{opacity:.5;cursor:not-allowed}.secondary,.secondary-link{background:#fff!important;color:#315fbd!important;border:1px solid #d9e0e9!important}.success-head{display:flex;justify-content:space-between;gap:20px;align-items:flex-start}.eyebrow{font-size:10px;font-weight:800;letter-spacing:.08em;color:#315fbd}.state-pill{padding:7px 10px;border-radius:999px;background:#ecfdf5;color:#047857;font-size:10px;font-weight:800;white-space:nowrap}.result-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin-top:20px}.result-grid>div{padding:14px;border:1px solid #d9e0e9;border-radius:10px;background:#fff}.result-grid small,.result-grid strong,.result-grid span{display:block}.result-grid small{color:#68758a;font-size:9px}.result-grid strong{margin-top:6px;color:#172033;font-size:10px;overflow-wrap:anywhere}.result-grid span{margin-top:4px;color:#68758a;font-size:9px}.timeline{display:grid;margin-top:20px}.timeline div{position:relative;padding:16px 16px 16px 42px;border-left:2px solid #dfe5ed}.timeline div:before{content:'○';position:absolute;left:-10px;top:13px;background:#f4f6f9;color:#8a96a8;font-size:20px}.timeline .done:before{content:'✓';font-weight:800;color:#059669}.timeline b,.timeline small{display:block}.timeline small{margin-top:4px;color:#68758a;font-size:10px}.status{margin-top:12px;font-size:10px}.error{color:#b91c1c}@media(max-width:850px){.result-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:650px){.form{grid-template-columns:1fr}.heading,.success-head{flex-direction:column}.result-grid{grid-template-columns:1fr}}`]
})
export class PrepareRealWorkspacePage {
 private readonly service=inject(RealWorkspaceService); options:RealWorkspaceOptions|null=null; result:RealWorkspaceResult|null=null; step=1; useCase=''; templateKey=''; industry=''; region=''; countries=''; name=''; dailyDiscoveryLimit=25; minimumScore=70; saving=false; status=''; error=false;
 ngOnInit(){this.service.options().subscribe({next:x=>this.options=x,error:()=>this.fail('Could not load workspace options.')});}
 get templates(){return (this.options?.templates||[]).filter(x=>!this.useCase||x.useCaseId===this.useCase);}
 get ready(){return !!this.useCase&&!!this.templateKey&&!!this.industry&&!!this.region;}
 selectArea(id:string){this.useCase=id;this.templateKey='';this.step=2;}
 start(){if(!this.ready)return;this.saving=true;this.status='';this.error=false;this.service.prepare(this.request()).subscribe({next:x=>{this.result=x;this.step=3;this.saving=false;this.status='Workspace automation queued successfully.'},error:e=>{this.saving=false;this.fail(e?.error?.detail||e?.error?.error||'Could not start workspace automation.')}});}
 private request():RealWorkspaceRequest{return {tenantId:'',name:this.name||null,useCase:this.useCase,templateKey:this.templateKey,industry:this.industry,region:this.region,countriesJson:JSON.stringify(this.countries.split(',').map(x=>x.trim()).filter(Boolean)),dailyDiscoveryLimit:this.dailyDiscoveryLimit,minimumScore:this.minimumScore};}
 private fail(message:string){this.status=message;this.error=true;}
}
