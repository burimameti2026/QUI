import { CommonModule } from '@angular/common';
import { Component, OnInit } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { PageHeader } from '../../shared/ui';
import { ModuleAdminService, TenantAlert } from './module-admin.service';

@Component({standalone:true,imports:[CommonModule,PageHeader],styles:[`.toolbar{display:flex;justify-content:flex-end;margin-bottom:12px}.alert{border-left:3px solid var(--line);padding:12px;margin-bottom:8px}.critical{border-left-color:#dc2626}.warning{border-left-color:#d97706}.info{border-left-color:#2563eb}.row{display:flex;justify-content:space-between;gap:12px}.meta{font-size:8px;color:var(--muted);margin-top:4px}.badge{font-size:8px;text-transform:uppercase;font-weight:800;padding:3px 8px;border-radius:999px;background:#f3f4f6}.ack{opacity:.55}button{cursor:pointer}`],template:`<qai-page-header title="Tenant alerts" subtitle="Active failures and lifecycle notifications requiring administrator attention."></qai-page-header><div class="toolbar"><button (click)="load()">Refresh</button></div><section class="panel" *ngIf="loaded;else loading"><article *ngFor="let a of alerts" class="alert" [class]="a.severity" [class.ack]="a.acknowledged"><div class="row"><div><b>{{a.message}}</b><div class="meta">{{a.type}} · {{a.createdAtUtc|date:'medium'}}</div></div><div><span class="badge">{{a.severity}}</span> <button *ngIf="!a.acknowledged" (click)="ack(a)">Acknowledge</button></div></div></article><div *ngIf="!alerts.length" class="meta">No alerts recorded.</div></section><ng-template #loading><section class="panel">Loading alerts…</section></ng-template>`})
export class TenantAlertsPage implements OnInit{tenantId='';alerts:TenantAlert[]=[];loaded=false;constructor(private route:ActivatedRoute,private data:ModuleAdminService){}ngOnInit(){this.tenantId=this.route.snapshot.paramMap.get('tenantId')||'';this.load()}load(){if(!this.tenantId)return;this.loaded=false;this.data.alerts(this.tenantId).subscribe({next:r=>{this.alerts=r;this.loaded=true},error:()=>{this.alerts=[];this.loaded=true}})}ack(a:TenantAlert){this.data.acknowledgeAlert(this.tenantId,a.id).subscribe(()=>this.load())}}
